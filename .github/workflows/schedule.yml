name: Scheduled Playwright Tests

on:
  schedule:
    - cron: '0 14 * * *' # 2:00 PM UTC
    - cron: '0 18 * * *' # 6:00 PM UTC

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      # Runs ONLY at 11 AM UTC
      - name: Run 11 AM Tests (@authentication)
        if: github.event.schedule == '0 14 * * *'
        run: npx playwright test -g "@authentication"

      # Runs ONLY at 6 PM UTC
      - name: Run 6 PM Tests (@account)
        if: github.event.schedule == '0 18 * * *'
        run: npx playwright test -g "@account"

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14